function projectCreation(input) {
    let architectName = input[0];
let projects = input[1];
let timeNeeded = projects * 3;
console.log("The architect " + architectName + " will need " + timeNeeded + " hours to complete " + projects + " project/s.");
}